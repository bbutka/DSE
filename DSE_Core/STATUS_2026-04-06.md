# DSE_Core Status

Date: April 6, 2026

## Current integrated flow

The active integrated package flow is now:

1. Phase 1: ILP by default via `dse_tool.agents.ilp_phase1_agent.ILPPhase1Agent`
2. Phase 1 fallback: ASP/Clingo via `dse_tool.agents.phase1_agent.Phase1Agent`
3. Phase 2: Clingo via `Clingo/zta_policy_enc.lp`
4. Phase 3: Clingo via `Clingo/resilience_enc.lp`
5. Runtime: optional tc9-specific extension after or alongside Phase 2

## What was completed

- Switched the active Phase 1 model to the multiplicative risk formulation.
- Added ILP Phase 1 to `DSE_Core` and made it the default backend.
- Kept ASP Phase 1 as an automatic fallback path.
- Aligned Phase 2 and Phase 3 to the integrated generated-topology flow.
- Fixed Phase 1 ILP/ASP parity on the checked integrated cases:
  - `tc9`: `max_security`, `balanced`, `min_resources`
  - `RefSoC`: `max_security`
- Updated runtime, reporting, GUI, and parser paths to match the current Phase 1/2/3 contracts.
- Quarantined the old standalone legacy path under `legacy/`.

## Legacy quarantine

The following files are no longer part of the active integrated package path:

- `legacy/runners/runClingo.py`
- `legacy/runners/runClingo_tc9.py`
- `legacy/runners/runClingo_darpa_uav.py`
- `legacy/Clingo/resilience_tc9_enc.lp`
- `legacy/testCases/testCase9_inst.lp`

These remain only for historical reference and manual comparison.

## Validation status

Validated under Python 3.12.

Most recent integrated suite run:

- `py -3.12 -m unittest tests.test_runtime tests.test_regression tests.test_golden_baselines -v`

Result:

- 179 tests passed
- 2 slow golden-baseline suites skipped by design unless `DSE_RUN_SLOW_GOLDEN=1`

## Remaining limitations

- Runtime remains tc9-specific by design.
- Legacy compatibility tolerance still exists for:
  - `ungovernerd_pep` parser support
  - `max_bufg` alias support
- Slow golden-baseline strategies are not exercised in the default regression run.

## Repository note

This status snapshot now lives inside the tracked `DSE` repository as
`DSE_Core/`. The original working copy used during cleanup was
`D:\DSE\DSE_Core`.
